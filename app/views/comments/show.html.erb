<div class="page-header">
  <h3>Comment #<%= @comment.id %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <dt>Commenter</dt>
      <dd>
        <% if @comment.user_with_makes_comments.present? %>
          <a href="/users/<%= @comment.commenter_id %>">
            <%= @comment.user_with_makes_comments.username %>
          </a>
        <% end %>
      </dd>

      <dt>Photo</dt>
      <dd>
        <% if @comment.photo_with_comments.present? %>
          <a href="/photos/<%= @comment.photo_id %>">
            <%= @comment.photo_with_comments.caption %>
          </a>
        <% end %>
      </dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/comments" class="btn btn-primary">
        Back
      </a>
      <% if current_user == @comment.user_with_makes_comments %>
        <a href="/comments/<%= @comment.id %>/edit" class="btn btn-warning">
          Edit
        </a>
        <a href="/delete_comment/<%= @comment.id %>" class="btn btn-danger" rel="nofollow">
          Delete
        </a>
      <% end %>
    </div>
  </div>
</div>
